# NOTES.md — 申し送り

作業中に気づいた懸念・改善案・判断の経緯を、その場で直さずにここへ記録する。

## 追記のルール

- **その場で直さなくてよい。記録して申し送る。** 本筋の作業を中断しない
- **「なぜそうしたか」を必ず書く。** ルールだけ残すと、後で誰かが善意で巻き戻す
- 一度決めた方針を変更提案する場合は、**理由を明示する**
- 確定した内容は `CLAUDE.md` の §3（設計上の確定事項）または §5（既知の課題）へ昇格させ、
  ここからは消さずに「→ CLAUDE.md §3 へ昇格」と追記する
- 事実と推測を書き分ける。推測には「推測:」と明示する

記入形式:

```
### YYYY-MM-DD <一行の見出し>
- 事実 / 推測:
- なぜ:
- どうするか（保留 / 要判断 / 次フェーズで対応）:
```

---

## 懸念・改善案

### 2026-08-01 外注スタッフにも全開催日を出す（決定）

- 事実: オーナー判断により **(a) 全スタッフに全開催日を出す** で確定（2026-08-01）。
  スタッフごとの担当日による絞り込みは行わない
- なぜそうしたか: 仕様書「確定構成 v1」にスタッフごとの対象日という概念が無く、
  絞り込みを入れるとスプレッドシートに割り当ての持ち方を追加する必要がある。
  全日出して事務局が決定時に振り分ける運用で足りると判断された
- 影響: **Phase 1 の「回答行の事前生成」は、イベント × 開催日 × 全対象スタッフ**
  の総当たりで生成すればよい。絞り込み条件は不要
- 副作用: 最大13日のイベントでは、外注スタッフも13日分すべてに回答することになる。
  **未回答ガード（カウンタ・破線枠・未回答日への自動スクロール）は省略できない。**
  提出後はスタッフ自身で修正できない仕様のため、見落としの代償が大きい
- → **CLAUDE.md §3 へ昇格済み**

### 2026-08-01 日程グリッドは13日でも縦に伸びないことを実測で確認

- 事実: 開催日を13列にして実測。横スクロール量 1,448px（約4.7画面分）に対し、
  **グリッドの高さ 275px・ページ全体 1,220px は5日のときと変わらない**（2026-08-01 検証）
- なぜ記録するか: 日数が増えても他セクションの位置が動かないため、
  最大13日という条件でもレイアウトは破綻しない。縦並びへの切り替えは不要と判断した
- 補足: 13日では未回答の日をスクロールで探すのが負担になるため、
  **「次へ」を押すと最初の未回答日へ自動スクロールする仕組みが必須**（実装済み）。
  提出後はスタッフ自身が修正できない仕様のため、未回答のまま提出させない手当ては外せない

### 2026-08-01 参考画像（dribbble 音楽アプリ）は事務用途に転用しにくい

- 事実: オーナーが参考画像を提示。3画面とも、各行を**アルバムアートのサムネイルが支える**
  構成で、曲名・アーティスト名の文字自体は小さくコントラストも低い。1画面あたり7行程度で、
  表も数値比較も存在しない
- なぜ問題か: イベント運営にはサムネイルに相当する画像が無い。画像を抜くと行の区切りと
  視線誘導を失い、平坦なテキストの羅列になる（design-1 の確定リストで実際に発生）。
  また管理画面はスタッフ42名 × 複数列 + 室数照合で、参考画像に対応する画面が無い。
  音楽アプリは「長く滞在させる」設計、事務作業は「速く終わらせる」目的で、
  広い余白と大きな角丸がスクロール量の増加として跳ね返る
- 判明したこと: **スタッフ回答画面だけは相性が良い**（項目が少ない・1画面1アクション・
  モバイル・低頻度）。噛み合わないのは確定リストと管理画面の2画面に集中している
- どうするか: オーナーが別案を検討中（2026-08-01 時点）。design-1 は
  `design-explore/design-1/` に残し、別案は `design-2/` として横に並べて見比べる。
  → **採用案が決まったら CLAUDE.md §3 へ理由とともに昇格させる**

### 2026-08-01 外部 .js は file:// で開いたページでは実行されない

- 事実: `<script src="js/prototype.js">` を file:// のページから読み込んだところ、
  スクリプトが実行されなかった（ボタンが反応せず、`click()` を直接呼んでも
  ハンドラが未登録だった）。HTML にインライン化して解決
- なぜ問題か: プロトタイプを「ダブルクリックで開くだけ」で確認できる必要がある
- どうするか: 検討用プロトタイプではスクリプトをインラインで書く。
  **本実装は GitHub Pages の HTTP 配信になるためこの制約は無くなる**ので、
  そのときは通常どおり外部ファイルに分離してよい

### 2026-08-01 Browser ツールでの画面確認は動いた（ただし file:// のみ）

- 事実: CLAUDE.md §8 で「実行可否は未検証」としていた Browser ツールでの画面確認が動作した。
  表示・クリック・スクロール・DOM 参照・ビューポート変更すべて成功（2026-08-01）
- 未検証: **開発サーバー経由（localhost）は試していない。** 今回動いたのは file:// の静的表示のみ
- どうするか: CLAUDE.md §8 の「実行可否は未検証」は、デプロイを伴う変更のタイミングで
  「file:// は確認済み / localhost は未確認」に書き換える

### 2026-07-31 GAS の Web App 実行回数の上限を確認できていない

- 事実: Apps Script 公式クォータ表で「Email recipients per day = 100（無料）」等は確認したが、
  **Web App のユーザー起動による実行回数の上限は確認できなかった**
- なぜ問題か: 4桁コードの総当たり攻撃を受けたときに、何回で頭打ちになるかが分からない。
  B-4（総当たり耐性）の設計根拠が一つ欠けている
- どうするか: 要判断。Phase 1 の疎通確認と合わせて、実際のクォータ表示を確認する

### 2026-07-31 Session.getActiveUser() の Web App での挙動が未検証

- 事実: 公式の Web Apps ガイドに、Web App 内での `Session.getActiveUser()` の挙動の記載を
  見つけられなかった
- なぜ問題か: 管理者用デプロイを `executeAs = USER_ACCESSING` にしたのは、これで確実に
  メールアドレスが取れると見込んだため。取れなければ管理者判定の方式ごと作り直しになる
- どうするか: Phase 1 で最優先に検証する

### 2026-07-31 管理画面は許可メール照合を入れるまで全 Google ユーザーに開いている

- 事実: `access = ANYONE` は「ログイン済の任意のユーザー」であり、管理者限定ではない
- なぜ問題か: 許可メールリストの照合を実装するまで、URL を知っている全 Google ユーザーが
  管理画面の API を呼べる
- どうするか: 管理者用デプロイを作るのと**同じフェーズで**許可リスト照合を実装する。
  照合なしで管理者用デプロイを公開しない

### 2026-07-31 Cache による失敗カウンタは fail-open になる

- 事実: CacheService は最大 21,600 秒（6時間）で、それ以前に消失することもある
- なぜ問題か: 失敗カウンタが消えると**ロックが解除される方向**に倒れる。安全側ではない
- どうするか: 保留。まず遅延（1〜2秒）と全体レート制限で運用し、実際に攻撃を受けた形跡が
  出たら Properties への移行を検討する

### 2026-07-31 計算ロジックを Node.js からテストする方法が未決定

- 事実: GAS は `import` / `export` を持たない。`module.exports` を書くと GAS 側で邪魔になる
- なぜ問題か: 「純粋な計算ロジックだけ手元でテストする」方針の実現方法が決まっていない
- 推測: `fs.readFileSync` + `vm` でファイルを評価すれば、GAS 側に手を入れずにテストできる
- どうするか: Phase 2（集計ロジック）の着手時に方式を決める

### 2026-07-31 GitHub Pages の公開元をリポジトリルートにした

- 事実: 無料プランの GitHub Pages が選べる公開元は `main / (root)` か `main / docs` のみ。
  `docs/` はドキュメント置き場として使うため、フロントをルートに置いた
- 副作用: `CLAUDE.md`・`NOTES.md`・`docs/` も Pages 経由で配信されうる。
  ただし public リポジトリなので、元から公開されている情報であり実害の増加はない
- どうするか: 保留。ルートが散らかって耐えられなくなったら、GitHub Actions での公開に
  切り替える。ただし**開発者不在運用のため Actions への依存は増やしたくない**

### 2026-07-31 git リポジトリを初期化できていない

- 事実: `git init` が `.git: Operation not permitted` で失敗した（サンドボックスによる拒否）
- なぜ問題か: `.gitignore` が意図どおり効くかを `git status` / `git check-ignore` で
  検証できていない。**除外できているという確認が取れていない**
- どうするか: 要判断。オーナーがターミナルから `git init` を実行するか、
  サンドボックス設定を変更するかを決める
