# イベント運営システム 立ち上げプロンプト v1（Claude Code 用）

<変数>
PROJECT_ROOT   = <プロジェクトのフルパス。例: ~/Documents/dev/event-ops>
SPEC_FILE      = <確定構成 v1（本システムの仕様書）のフルパス>
CHECKLIST_FILE = <新規PJ_初期設計チェックリスト_v1.md のフルパス>
</変数>

**本プロンプトの対象は「立ち上げ」までである。** 具体的には、チェックリスト Part 1 の確認、CLAUDE.md の作成、リポジトリの骨組み、フェーズ計画の提示まで。**機能の実装は行わない。** 実装はフェーズごとに別プロンプトで依頼する。

---

<最優先事項>
以下は他のどの指示と衝突しても優先する。

1. ファイルの削除・`git push`・`git reset --hard` は行わない。既存ファイルの上書きが必要になった場合は、変更せず報告して止まる。
2. **秘密情報をリポジトリに書かない。** 4桁コード、スタッフの氏名・メールアドレス、スプレッドシートの内容、GAS の Web App URL、認証用の値。**`.gitignore` を作る前にこれらを含むファイルを作らない。**
3. 各STEPの終わりで必ず止まる。承認を得るまで次のSTEPに進まない。**承認が必要なのはSTEP境界のみ**で、1つのSTEP内に並ぶ番号付き作業は連続して実行してよい。
4. 確認できなかったことを、確認できたように書かない。**特に、実際に動かして確かめていない挙動を「動く」と書かない。**

**各STEPの報告の最終行に、必ず以下いずれか1行を出力する。**
```
STEP_DONE: <STEP番号> / files=<N> / 未定項目=<N> / 未検証=<1行>
STEP_BLOCKED: <STEP番号> / reason=<1行> / 再開に必要なもの=<1行>
```
該当しない項目は `-` を入れる。
</最優先事項>

<最初に読むもの>
1. `{SPEC_FILE}`（確定構成 v1）を全文読む。
2. `{CHECKLIST_FILE}`（初期設計チェックリスト）を全文読む。**Part 4「環境の既知制約」は検証済みの事実として扱ってよい。** 再検証しない。

**チェックリスト本体を CLAUDE.md に貼らないこと**（同書 23行目）。立ち上げ時に一度使うものである。
</最初に読むもの>

<範囲>
対象: `{PROJECT_ROOT}` 配下のみ。存在しなければ新規作成する。
読み取り専用: `{SPEC_FILE}` / `{CHECKLIST_FILE}`
触ってはいけない: `{PROJECT_ROOT}` の外にある全てのファイル

やらないこと（本プロンプトの範囲外）:
- 機能の実装（GAS のロジック、画面、メール送信、集計）
- Google アカウントの作成、GAS プロジェクトの作成、スプレッドシートの作成
- GitHub リポジトリの作成、`git push`、GitHub Pages の設定
- ホテル提出ファイルの様式設計
- ルートイン連携（仕様書で初期対象外）
</範囲>

---

<STEP 0: 確認と調査>

## A. チェックリスト Part 1 の質問

`{CHECKLIST_FILE}` の Part 1（1-1〜1-8）の項目を確認する。**ただし `{SPEC_FILE}` を読めば答えが分かる項目は質問しない。** 仕様書から読み取れた答えを先に一覧で示し、**読み取れなかった項目だけを質問する。**

質問は **1回のメッセージにまとめて出す。1問ずつ聞かない。** 答えられない項目は「未定」として記録し、無理に埋めない。

## B. 仕様書だけでは決まらない事項

以下は仕様書と Part 4 の既知制約を突き合わせて判明した、**このまま実装に入ると詰まる可能性がある論点**である。調査した上で選択肢を提示し、**自分で決めない。**

**B-1. Gmail の送信上限**（最優先）

督促メールを未回答者へ一斉送信する設計だが、**新規に作る無料 Google アカウントの Gmail には1日あたりの送信上限がある。** スタッフ数と送信頻度によっては上限に当たり、督促が途中で止まる。

- GAS から送る場合の実際の上限値を、**公式ドキュメントで確認する**（Apps Script のクォータ、Gmail の送信制限）。**個人ブログを根拠にしない。**
- 想定スタッフ数を質問し、上限に収まるかを数値で示す
- 収まらない場合の選択肢を挙げる（分割送信、Google Workspace への切り替え、外部メール配信サービス等）
- **確認できなかった値は「確認できず」と書く**

**B-2. GitHub Pages から GAS Web API を呼べるか**

ブラウザから別ドメインの GAS Web App を呼ぶ際、CORS プリフライト（OPTIONS リクエスト）の扱いが問題になる可能性がある。**GAS の Web App が OPTIONS に応答できるかを調査する。**

- 応答できない場合、プリフライトを発生させない呼び出し方（Content-Type の選択等）で回避できるかを調べる
- **これは「たぶん大丈夫」で進めてはいけない。** 動かないと画面から API を一切叩けず、構成が根本から成立しない
- 調査で確定できない場合は、**最小の疎通確認を最初の実装タスクにする**ことを提案する

**B-3. 管理者認証と一般アクセスの両立**

仕様書は「スタッフ認証 = 共通URL＋4桁コード」「管理者認証 = Googleアカウント」としている。GAS Web App の公開設定は「全員」か「自分のみ」等の択一のため、**この2つをどう両立させるかが決まっていない。**

- デプロイを2つに分ける、実行ユーザー設定を使い分ける等、実際に取りうる方式を調べて提示する
- **GitHub Pages は公開されるため、フロントの JavaScript に書いた GAS の URL は誰でも見られる。** 事実上、4桁コードだけが防御になる点を明記する

**B-4. 4桁コードの総当たり耐性**

4桁 = 10,000通り。仕様書は「連続失敗時は一時停止」としているが、**GAS の Web App はリクエストごとに状態を持たないため、失敗回数をどこに記録するかを決める必要がある**（Properties Service / Cache Service / スプレッドシート）。

- 何を基準に数えるか（IP は GAS から取得できない可能性がある。取得可否を調査する）
- 記録先ごとの制約（保存期間、書き込み回数の上限）

**B-5. スプレッドシートへの同時書き込み**

複数のスタッフが同時に回答すると、行の追記が競合しうる。**排他制御の方式**（LockService 等）と、失敗時の挙動を決める必要がある。

## C. 追加で報告すること

- `{PROJECT_ROOT}` が既に存在するか。存在する場合は中身の一覧
- `git` の有無とバージョン
- 上記 A・B 以外に、仕様書とチェックリストの突き合わせで見つかった要決定事項

**調査は Web検索 8クエリ以内 / 閲覧 12ページ以内。** 一次情報（Google の公式ドキュメント）を優先し、個人記事を根拠にする場合は「個人記事」と明記する。

**ここで止まる。** 回答を得るまで STEP 1 に進まない。
</STEP 0>

---

<STEP 1: 骨組みの作成>

STEP 0 の回答を反映して以下を作る。**機能の実装は含めない。**

1. **`.gitignore` を最初に作る。** チェックリスト 1-4 に従い、以下を確実に除外する。
   - スタッフデータ、メールアドレス一覧、4桁コードを含みうるファイル
   - GAS の Web App URL や設定値を書くファイル（**実ファイル名を列挙する。`*.env` のようなワイルドカードは `.env.example` を巻き込むため使わない**。Part 4 の既知制約）
   - ホテル提出ファイル、スプレッドシートの書き出し

2. **`CLAUDE.md` を作る。** チェックリスト Part 3 の骨組み（1〜8）に従う。**200行以内。** 超える場合は手順書を別ファイルに分け、参照のみにする。
   - §1 に**他プロジェクトと取り違えないための注記**を入れる（`fg-event-platform` / `fdj-inventory` と並存するため）
   - §2「絶対に守ること」に、**GAS は push で自動反映されない**旨と、手動デプロイが必要な箇所を明記する（チェックリスト 1-7）
   - §3・§4・§5 は**空欄の見出しだけ**作る（Part 2）。今は埋めない
   - §7 に「実装が正、ドキュメントは参考」の優先順位を書く

3. **`NOTES.md` を作る。** 申し送り用。空の見出しのみ。追記ルールをチェックリスト Part 2 から転記する。

4. **ディレクトリ構成を作る。** 仕様書の構成（GitHub Pages のフロント / GAS のソース）に対応する空のディレクトリと、各ディレクトリの役割を書いた1行の README を置く。**コードは書かない。**

5. **`docs/` に秘密情報の置き場所の対応表を作る。** 何をどこに置くか（GAS の Properties / スプレッドシート / ローカルのみ）を表にする。**値そのものは書かない。**

6. **フェーズ計画を提示する。** 仕様書の業務フローを、実装の順序に落とす。各フェーズについて以下を1行ずつ書く。
   - 何を作るか
   - **それを誰がどうやって検証するか**（チェックリスト 1-3。localhost が塞がれているため、Claude が自力で検証できない部分を明示する）
   - 前のフェーズの何に依存するか

**検証（あなたが実行し、結果を報告する）**
- `git status` で、秘密情報を含むファイルが追跡対象になっていないことを確認する
- `.gitignore` に書いた各パターンについて、**実際にマッチするファイル名の例と、意図せず巻き込むファイルが無いか**を確認する
- CLAUDE.md の行数を報告する。200行を超えていたら分割する
- 作成したファイルの一覧（パス+行数）

**検証できないこと（明記して報告する）**
- GAS・スプレッドシート・GitHub Pages の実際の動作。**このSTEPでは何も動かしていない**
- Part 4 の既知制約により、**起動中アプリへの localhost 接続は OS 層で拒否される。** 画面の動作確認は人間が行う

**ここで止まり、報告する。**
</STEP 1>

---

<STEP 2: 引き継ぎメモ>
`{PROJECT_ROOT}/docs/` に日付入りの引き継ぎメモを新規作成する。既存ファイルに追記しない。
- 作成したファイルの一覧
- STEP 0 で決まった事項と、その理由
- **「未定」のまま残した項目の一覧**（チェックリストは無理に埋めないことを求めている）
- **B-1〜B-5 のうち、調査で確定できなかったもの**。次に何を確認すれば決まるかを1行ずつ書く
- 提示したフェーズ計画と、次に着手すべきフェーズ
</STEP 2>

---

<推測の禁止>
- 事実と推測を分けて書く。推測には「推測:」と明示する
- **GAS・Gmail・GitHub Pages の仕様を推測で書かない。** 公式ドキュメントで確認できたものだけを事実として扱う
- 仕様書に書かれていない仕様を補完しない。必要なら止まって質問する
- 見つからなかったことは「見つからなかった」と書く。もっともらしい代替で埋めない
- **チェックリストで「未定」と答えられた項目を、勝手に決めない**
</推測の禁止>

<コスト制御>
- サブエージェントは **使わない**。`ultracode` も使わない
- 調査は STEP 0 の A・B 合わせて Web検索 8クエリ以内 / 閲覧 12ページ以内
- 各STEPの報告は **60行以内**。ファイルの全文を報告に貼らない（パスと行数で示す）
- ファイル読み込みは `{PROJECT_ROOT}` / `{SPEC_FILE}` / `{CHECKLIST_FILE}` に限る
- 同じSTEPで3回連続してエラーが解決しない場合、**そこで止めて状況を報告する**
- 質問は各STEPの終わりに1回だけ、まとめて行う
</コスト制御>
