# CLAUDE.md — MSC イベント運営システム

## 1. プロジェクト概要

**目的**: 外注スタッフの参加可否・宿泊希望を Web で収集し、事務局の参加者決定 → 確定リスト公開
→ ホテル提出名簿の出力までを一元化する。LINE で毎回一覧を手送りしていた運用の置き換え。

**使う人**: 外注スタッフ 30〜50名（最大100名想定・回答のみ）／ MSC 事務局（管理・決定）

**位置づけ**: 対外用。外注スタッフが直接触れるため見た目と分かりやすさが要件に入る。
ただし業務範囲内であり、どこまで作り込むかの塩梅はオーナーが判断する。

**技術スタック**（※ 実装が進んだら実測値に置き換えること。現時点は設計時点の予定）
| 層 | 採用 |
|---|---|
| フロント | GitHub Pages（public リポジトリ・静的 HTML / CSS / JavaScript） |
| バックエンド | Google Apps Script Web App（画面は持たず API・集計・メール・定期処理のみ） |
| データ | Google スプレッドシート（非公開） |
| ソース管理 | GitHub（public）＋ clasp（GAS ソースの同期） |
| メール | `noreply.msc.mgmt@gmail.com`（無料 Google アカウント・未作成） |

**起動方法**: 常時稼働。スタッフは共通 URL をブラウザで開く。ローカル開発時は静的ファイルを
開発サーバーで配信する（`docs/development.md` 参照）。

**運用形態**: **開発者不在での運用を前提とする。** 事務局が単独で年間運用できることを設計の
制約とする。スクリプトを叩かないと再現できない成果物・手順を作らない。

**他プロジェクトと取り違えないための注記**

このリポジトリは `shift-Ops` である。同じ `~/Documents` 配下に別プロジェクトが並存する。

- `fg-event-platform` — **別物。** 混同しないこと
- `fdj-inventory` — **別物。** 混同しないこと

作業前に必ずカレントディレクトリが `~/Documents/dev/shift-Ops` であることを確認する。

---

## 2. 絶対に守ること

### 2-1. このリポジトリは public である

無料プランでは GitHub Pages は public リポジトリでしか公開できない。
**ここにコミットしたものは全世界から読める。** 以下を絶対に書かない・置かない。

- 4桁の共通コード
- スタッフの氏名・メールアドレス・所属
- GAS Web App の URL、スクリプト ID、デプロイ ID
- 管理者の許可メールアドレス一覧
- スプレッドシートの ID および中身
- ホテル提出ファイル

新しく秘密情報を含むファイルを作るときは、**ファイルを作る前に `.gitignore` へ追記する。**
置き場所の対応表は `docs/secrets-placement.md` を見ること。

### 2-2. 破壊的操作の禁止

- ファイルの削除、`git push --force`、`git reset --hard` を実行しない
- 既存ファイルの上書きが必要になったら、**変更せず報告して止まる**
- 本番スプレッドシートを直接編集しない。開発用スプレッドシートで作業する

### 2-3. デプロイ前の確認

- `git push` は実行前に必ずオーナーの確認を取る
- **GAS は `git push` では反映されない。** 反映には次の3段が必要:
  1. `clasp push`（ソースを Drive へアップロード。**これだけでは公開中のアプリは変わらない**）
  2. `clasp version`（バージョンを作る）
  3. `clasp deploy`（デプロイを更新して初めて利用者に反映される）
- GitHub Pages への反映は `main` への push で自動。GAS とはタイミングがずれる。
  **フロントと GAS の両方を変えたときは、GAS を先に反映する**（新しい画面が古い API を叩くのを避ける）

### 2-4. 検証していないことを「動く」と書かない

実際に動かして確かめていない挙動を、確認済みとして報告しない。
確認できなかったことは「確認できなかった」と書く。

---

## 3. 設計上の確定事項（巻き戻し禁止）

過去に検討のうえ確定した判断を記録する。変更を提案する場合は理由を明示すること。

<!-- 追記のタイミング: 検討の末に方針を決めたとき。理由も必ず併記する -->

立ち上げ時点の決定は `docs/handoff_2026-07-31_v1.md` §2 を参照（未転記）。

### UI は design-2（黒 × 赤・CrewLink 風）を採用する（2026-08-01）

`design-explore/design-2/` を採用。**design-1（ダーク × 紫）は不採用。** 復活させないこと。

**理由**: design-1 の元にした参考画像（音楽アプリ）は各行をアルバムアートが支える構成で、
画像を持たないスタッフ名簿では行の区切りと視線誘導を失う。1画面7行程度を前提としており、
スタッフ42名 × 複数列の管理画面に対応する画面が無かった。
design-2 の元にした参考画像はイベントスタッフのシフト提出そのものを扱っており、構造が一致する。

**この決定に付随して確定したこと**
- **赤は用途で2本立てにする。** 線・文字・アイコンは `#FF3B45`、塗りボタンは `#E1121D` + 白文字。
  取り違えるとコントラスト基準を割る（深い赤を文字にすると 4.10:1、明るい赤の塗りに白文字は 3.52:1）
- 状態色は赤系と別系統にする（確定 `#3ED67F` / 未回答 `#FFB020` / エラー `#FF5A63`）
- 日程選択は横スクロール。**未回答ガードは省略しない**（下記の決定を参照）
- 詳細は `design-explore/design-2/DESIGN-NOTES.md`

### 回答対象は「全スタッフ × 全開催日」とする（2026-08-01）

スタッフごとに担当日を割り当てて絞り込むことはしない。全スタッフに全開催日を出し、
**事務局が参加者決定の段階で振り分ける。**

**理由**: 仕様書「確定構成 v1」にスタッフごとの対象日という概念が無く、絞り込みを入れると
スプレッドシートに割り当ての持ち方を追加する必要がある。全日出す運用で足りると判断した。

**この決定が効く場所**
- 回答行の事前生成は **イベント × 開催日 × 全対象スタッフ** の総当たりでよい。絞り込み条件は不要
- 最大13日のイベントでは外注スタッフも13日分に回答する。
  **未回答のまま提出させない仕組みを省略しない**（提出後はスタッフ自身で修正できないため）

---

## 4. 実装状況

「未実装」と「意図的に実装しない」を区別して書く。

<!-- 追記のタイミング: 機能を実装・断念したとき -->

### できていること

（なし。骨組みのみ）

### できていないこと

（全機能。フェーズ計画は `docs/phases_v1.md`）

---

## 5. 既知の課題・技術的負債

<!-- 追記のタイミング: 「後で直す」と判断したとき / 環境や仕様の制限にぶつかったとき -->

（`NOTES.md` の申し送りから、確定したものを昇格させる）

---

## 6. 命名規則・コミット規約・ブランチ運用

**ブランチ**: 開発段階ではブランチを切らない。`main` に直接コミットする。
運用開始後は「本番稼働中に壊せない変更」でのみブランチを切る。

**コミットメッセージ**: 日本語で「何を」「なぜ」。1行目は50文字以内。

**命名**
- ファイル・ディレクトリ: 小文字 + ハイフン（`hotel-export.js`）
- GAS の関数: キャメルケース（`buildHotelRoster`）
- スプレッドシートのタブ名: 仕様書の名称をそのまま使う（「スタッフ」「回答」等）
- 日付入りドキュメント: `<名前>_<YYYY-MM-DD>_v<N>.md`

---

## 7. ドキュメントの読み方

**実装が正、ドキュメントは参考。** 食い違ったらコードを信じる。

信頼度の高い順:

1. **動いているコード**（`gas/` と フロントの実ファイル）
2. **CLAUDE.md の §2・§3**（守るべきルールと確定事項）
3. `docs/` 配下の設計メモ・引き継ぎメモ
4. `NOTES.md`（未整理の申し送り。確定事項ではない）
5. 仕様書「確定構成 v1」（立ち上げ時点の設計。**実装で覆った箇所がある前提で読む**）

ドキュメントの更新は**デプロイを伴う変更のとき**に行う。コミット毎には更新しない。

---

## 8. 作業の進め方

1. **コードを読んで現状を把握する。** ドキュメントより先にコードを読む
2. 変更内容を要約し、コミットメッセージ案を提示する
3. `git push` と `clasp deploy` は実行前に確認を取る
4. 気づいた懸念・改善案は、その場で直さなくてよい。**`NOTES.md` に申し送る**
5. 一度決めた方針を変更提案するときは、**理由を明示する**

### 検証の分担

| 対象 | 誰が | 手段 |
|---|---|---|
| 集計・差分などの計算ロジック | Claude | Node.js のテスト（`test/`） |
| フロントの画面・操作 | Claude | 開発サーバー + ブラウザ（**実行可否は未検証**） |
| GAS の動作・メール送信・スプレッドシート | 人間 | 実環境で手動確認 |
| CORS 疎通（Pages → GAS） | 人間 | 実際の GAS デプロイに対して確認 |
| UI / UX の一次評価 | Claude | `docs/ux-criteria.md` の配点で採点。90/100 超で一次完成 |
| 最終的な完成検査 | オーナー | — |

**localhost / 127.0.0.1 への接続は macOS の OS 層で拒否される**（環境の既知制約）。
起動中アプリへの `curl` は通らない。画面確認はブラウザツールか人間が行う。
